# Implementation Plan - Extract 2027 Internships

Extract all internships/jobs from `intern.md` that are applicable to the 2027 batch and save them to `internship.md` manually.

## Proposed Changes

### Manual Extraction
I will perform the following steps:
1. Read the remaining content of `intern.md`.
2. Manually identify all entries where the "Batch" field includes "2027".
3. Extract the full block of details for each matching entry exactly as it appears.
4. Format the extracted entries into a clear list in `internship.md`.

## Verification Plan

### Manual Verification
1. Compare the contents of `internship.md` with `intern.md` to ensure every 2027 batch posting was captured.
2. Verify that the details, Links, and stipend information for each entry are copied correctly without modifications.
